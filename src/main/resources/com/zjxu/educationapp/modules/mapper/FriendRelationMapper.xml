<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zjxu.educationapp.modules.mapper.FriendRelationMapper">

    <resultMap id="BaseResultMap" type="com.zjxu.educationapp.modules.entity.FriendRelation">
            <id property="id" column="id" />
            <result property="userId" column="user_id" />
            <result property="friendId" column="friend_id" />
            <result property="status" column="status" />
            <result property="remark" column="remark" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,friend_id,status,remark,create_time,
        update_time
    </sql>
    <!-- 检查好友关系是否存在（双向检查） -->
    <select id="checkFriendRelation" resultType="int">
        SELECT COUNT(*)
        FROM friend_relation
        WHERE (user_id = #{userId} AND friend_id = #{friendId})
           OR (user_id = #{friendId} AND friend_id = #{userId})
    </select>

    <!-- 删除好友关系（双向删除） -->
    <delete id="deleteFriendRelation">
        DELETE FROM friend_relation
        WHERE (user_id = #{userId} AND friend_id = #{friendId})
           OR (user_id = #{friendId} AND friend_id = #{userId})
    </delete>
</mapper>
